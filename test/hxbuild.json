{
	"main": "Main",
	"classpath": ".",
	"include": ["assets/images", "assets/sounds"],
	
	"outFile": "Main",
	"outDir": "build",

	"libraries": ["haxe-files"],

	"configs:Release": {
		"defines": ["Release"],
		"optimize": "On",
		"outDir": "${outDir}/Release"
	},

	"configs:Debug": {
		"defines": ["Debug"],
		"optimize": "Off",
		"outDir": "${outDir}/Debug"
	},

	"targets:C++": {
		"target": "cpp",
		"outDir": "${outDir}/cpp",
		"libraries": ["hxglfw", "hxgl"]
	},

	"targets:HashLink": {
		"target": "hl",
		"outDir": "${outDir}/hl",
		"libraries": ["hlsdl"]
	},

	"targets:JavaScript": {
		"target": "js",
		"outDir": "${outDir}/js"
	}
}